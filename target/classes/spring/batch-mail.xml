<?xml version="1.0" encoding="UTF-8"?>
<!--
  batch-mail.xml
  ==============
  Spring JavaMailSender + EmailNotificationService beans.

  All SMTP settings are resolved from application.properties
  (or profile / external overrides).

  To disable email entirely: remove the ref to emailNotificationService
  from batch-listeners.xml, or set the property to null — the tasklet
  checks for null and silently skips the send.
-->
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="
           http://www.springframework.org/schema/beans
               https://www.springframework.org/schema/beans/spring-beans.xsd">

    <!-- ================================================================
         JavaMailSender — backed by Jakarta Mail (angus-mail)
         ================================================================ -->
    <bean id="mailSender"
          class="org.springframework.mail.javamail.JavaMailSenderImpl">
        <property name="host"     value="${mail.smtp.host}"/>
        <property name="port"     value="${mail.smtp.port}"/>
        <property name="username" value="${mail.smtp.username}"/>
        <property name="password" value="${mail.smtp.password}"/>
        <property name="javaMailProperties">
            <props>
                <prop key="mail.smtp.auth">          ${mail.smtp.auth}</prop>
                <prop key="mail.smtp.starttls.enable">${mail.smtp.starttls.enable}</prop>
                <prop key="mail.smtp.ssl.trust">      ${mail.smtp.ssl.trust}</prop>
                <prop key="mail.debug">               ${mail.debug:false}</prop>
            </props>
        </property>
    </bean>

    <!-- ================================================================
         EmailNotificationService
         Used by StatisticsAndEmailTasklet to send the HTML report.
         ================================================================ -->
    <bean id="emailNotificationService"
          class="com.example.batch.service.EmailNotificationService">
        <property name="mailSender"   ref="mailSender"/>
        <property name="fromAddress"  value="${mail.from}"/>
    </bean>

</beans>
